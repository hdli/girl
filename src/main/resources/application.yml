########选择激活对应环境上的配置文件
spring:
  profiles:
    active: dev
########数据库配置，配在此处所有环境都可用
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dbgirl
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true